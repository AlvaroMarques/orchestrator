[project]
name = "orchestrator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "black>=25.12.0",
    "graphviz>=0.21",
    "pytest>=9.0.2",
]

[project.scripts]
orchestrator="orchestrator.__main__:main"
